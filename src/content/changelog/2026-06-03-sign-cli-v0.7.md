---
title: "sign-cli v0.7 — verification is now cryptographic (and a security hardening pass)"
date: 2026-06-03
cli: sign-cli
version: "0.7.0"
summary: "A security release with breaking behavior. PDF signature verification now checks the PKCS#7 signature value against the signer certificate's public key (RSA/ECDSA), not just the /ByteRange digest — so forged or tampered signatures that previously reported ok now correctly fail. Plus the MCP server binds to loopback by default, profile secrets are auth-gated, the default provider is now the offline local PAdES signer, and 0.7.1 ships standalone binaries for macOS/Linux/Windows."
tags: ["sign-cli"]
---

**Heads-up: this release changes behavior in ways you should read before upgrading.** A signed PDF that previously verified `ok` against a *non-cryptographic* or *forged* signature will now correctly **fail** — that is the fix, not a regression.

### Verification is now genuinely cryptographic (breaking)

Through 0.6.x, `verify` / `request verify-signed-pdf` (and the `pdf_inspect_signatures` MCP tool) only recomputed the SHA-256 over the PDF's `/ByteRange` and compared it to the `messageDigest` embedded in the PKCS#7 — it **never verified the signature value itself** against the signer certificate's public key. A fully forged PKCS#7 with no private key could report `verdict=ok`.

0.7.0 performs real RSA/ECDSA verification (`createVerify`) of the signed attributes against the signer cert's public key. Forged and tampered signatures now fail as they should. If a PDF you relied on suddenly fails, it was never cryptographically signed.

### MCP & provider hardening (breaking)

- **MCP HTTP server binds `127.0.0.1` by default** (was `0.0.0.0`). Pass `--http-bind 0.0.0.0` to expose it; the hosted container entrypoint already does, so Railway/hosted deployments are unaffected.
- **`profile_show {show_secrets:true}`** now requires authentication — over HTTP that means a configured `--http-auth-token`. Provider API keys are no longer reachable unauthenticated.
- **Default provider is now the offline `local` PAdES signer** (was Dropbox Sign). A brand-new user's first `request create`/`send` no longer demands hosted-provider credentials — "no signup, no keys" works out of the box. Resolution order is unchanged; only the final fallback did.
- **`signer_fetch_document`** absolute output paths now require `SIGN_ALLOW_ABSOLUTE_DOCS=1` and are refused in `--read-only`.

### 0.7.1 — standalone binaries

`v0.7.1` is build-tooling only (no runtime change) and attaches standalone executables to the [GitHub release](https://github.com/DrBaher/sign-cli/releases) — `sign-darwin-arm64`, `sign-linux-x64`, `sign-win32-x64.exe` — for running sign-cli without a Node install.
