---
export interface Props {
  data: Record<string, any>;
}
const { data } = Astro.props as Props;
---
<script type="application/ld+json" set:html={JSON.stringify({ '@context': 'https://schema.org', ...data })} is:inline></script>
