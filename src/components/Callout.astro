---
export interface Props {
  /** Eyebrow label like "TL;DR" or "Heads up". */
  label?: string;
  /** Visual flavor. `info` is the default green; `warn` is amber. */
  variant?: 'info' | 'warn';
}
const { label = 'TL;DR', variant = 'info' } = Astro.props as Props;
const cls = variant === 'warn'
  ? 'border-l-2 border-amber-600 bg-amber-50 dark:bg-amber-900/20 dark:text-amber-100'
  : 'border-l-2 border-accent-600 bg-mint-50 dark:border-accent-400 dark:bg-accent-900/25';
---
<div class={`my-6 rounded-r px-5 py-3.5 text-ink-800 dark:text-night-50 ${cls}`}>
  <strong class="font-semibold text-ink-900 dark:text-night-50">{label} —</strong>
  <span class="ml-1"><slot /></span>
</div>
